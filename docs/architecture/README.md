# 🏗️ Architecture

This section contains system architecture documentation and diagrams.

---

## 📁 Files

| File | Description |
|------|-------------|
| [diagrams.md](./diagrams.md) | Architecture diagrams (Mermaid) |
| [data-models.md](./data-models.md) | Database schema and data models |

---

## 🌍 System Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           SMART BUILDING IOT PLATFORM                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────┐    ┌────────────────┐    ┌────────────────┐          │
│  │   IoT Devices  │    │  LoRaWAN       │    │  ROS Robots    │          │
│  │   (MQTT)       │    │  (ChirpStack)  │    │  (MQTT)        │          │
│  └───────┬────────┘    └───────┬────────┘    └───────┬────────┘          │
│          │                      │                      │                     │
│          └──────────────────────┼──────────────────────┘                     │
│                                 │                                            │
│                                 ▼                                            │
│                    ┌────────────────────────┐                                │
│                    │     MQTT Broker        │                                │
│                    │    (Mosquitto)         │                                │
│                    └───────────┬────────────┘                                │
│                                │                                            │
│          ┌─────────────────────┼─────────────────────┐                      │
│          ▼                     ▼                     ▼                      │
│  ┌───────────────┐    ┌───────────────┐    ┌───────────────┐              │
│  │   Node-RED    │    │  Node.js API  │    │  WebSocket    │              │
│  │   (Flows)     │    │   (Backend)    │    │  (Real-time)  │              │
│  └───────┬───────┘    └───────┬───────┘    └───────────────┘              │
│          │                    │                                            │
│          └──────────┬──────────┘                                            │
│                     ▼                                                       │
│            ┌───────────────┐                                                │
│            │  PostgreSQL   │                                                │
│            │  + TimescaleDB │                                                │
│            └───────┬───────┘                                                │
│                    │                                                        │
│                    ▼                                                        │
│            ┌───────────────┐    ┌───────────────┐                          │
│            │  React App    │    │    Auth0      │                          │
│            │  (Frontend)   │    │ (Authentication)│                         │
│            └───────────────┘    └───────────────┘                          │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 📡 Data Flows

### 1. Sensor Data (IoT → Platform)
```
IoT Device → MQTT → Node-RED → PostgreSQL → API → React Dashboard
```

### 2. Robot Control (Platform → ROS)
```
React Dashboard → API → Node-RED → MQTT → ROS Robot
```

### 3. LoRaWAN Data
```
LoRaWAN Device → Gateway → ChirpStack → MQTT → Node-RED → PostgreSQL → API → Dashboard
```

---

## 🔧 Component Responsibilities

| Component | Responsibility |
|-----------|----------------|
| **React App** | User interface, dashboard, device management |
| **Node.js API** | REST endpoints, business logic, authentication |
| **PostgreSQL** | Persistent storage, time-series data |
| **Node-RED** | Automation flows, protocol bridging |
| **MQTT Broker** | Device communication, pub/sub messaging |
| **ChirpStack** | LoRaWAN network management |
| **Auth0** | User authentication & authorization |

---

## 🔌 API Communication

| Path | Method | Description |
|------|--------|-------------|
| `/api/devices` | GET/POST | Device CRUD |
| `/api/readings` | GET/POST | Sensor readings |
| `/api/alerts` | GET/PUT | Alert management |
| `/api/auth/*` | * | Authentication |
| WebSocket | WS | Real-time updates |

---

*See [diagrams.md](./diagrams.md) for Mermaid diagrams.*
